---
- name: Wait until CouchDB is ready
  ansible.builtin.uri:
    url: "http://127.0.0.1:{{ couchdb_http_port }}/_up"
    method: GET
    status_code: 200
    timeout: 10
  register: couchdb_health
  retries: 12
  delay: 10
  until: couchdb_health.status == 200